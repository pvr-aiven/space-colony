# Space Colony — backend

Node.js + TypeScript + Fastify API, backed by Aiven for PostgreSQL.

See the [root README](../README.md) for local dev setup, Docker Compose
testing, and Aiven deployment steps — this file used to duplicate that
content; it now just points there to avoid the two drifting apart.
